// Write a function that takes two values, say a and b, as arguments. Return true if the two values are equal and of the same type

const comparison = (a, b) => a === b ? true : false;

console.log(comparison(2,true))